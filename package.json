{
  "name": "performance-trap-server",
  "version": "1.0.0",
  "description": "Performance Trap astrology reading server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check companion.js && node --check kids-on-the-bus/server.js && node --check kids-on-the-bus/lib/claude.js && node --check kids-on-the-bus/lib/latency.js && node --check kids-on-the-bus/lib/safety.js && node --check kids-on-the-bus/lib/usage.js && node --check kids-on-the-bus/public/turn-coordinator.js && node --check kids-on-the-bus/public/relay-fidelity.js && node --check kids-on-the-bus/public/app.js",
    "test": "node --test kids-on-the-bus/test/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
